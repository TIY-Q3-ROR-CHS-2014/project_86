%h1 Restaurant's Index

/ .row
/   - @restaurants.each do |restaurant|
/     .col-sm-3
/       .panel.panel-primary
/         .panel-heading
/           %h3.panel-title
/             = link_to restaurant.name, restaurant_path(restaurant)
/         .panel-body
/           = restaurant.description
/       / = link_to restaurant.name, restaurant_path(restaurant)
/       / |
/       / = link_to "Edit", edit_restaurant_path(restaurant)
/       / |
/       / = link_to "Delete", restaurant_path(restaurant), method: :delete
/   .col-sm-3
/     .panel.panel-default
/       .panel-heading
/         %h3.panel-title
/           = link_to "Add Restaurant", new_restaurant_path
/       .panel-body

.row
  .col-sm-4
    .list-group
    - @restaurants.each do |restaurant|
      %a.list-group-item{href: restaurant_path(restaurant)}
        %h4.list-group-item-heading
          = restaurant.name
        %p.list-group-item-text= restaurant.description
    %a.list-group-item.active{href: new_restaurant_path}
      %h4.list-group-item-heading= "Add Restaurant"
      %p.list-group-item-text
  
= link_to "Back", root_path


    


