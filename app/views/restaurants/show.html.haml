%h1.text-center
  =@restaurant.name

%div.row
  %div.col-lg-4
    - @restaurant.menus.each do|menu|
      = menu.name
      = menu.description
      %ul
      - menu.items.each do |item|
        %li
        = item.name
        = item.price


  %div.col-lg-8
    %h3.text-center
      Tables Go Here



  